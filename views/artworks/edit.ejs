<%-include("../partials/header")%>
<div class="section">
    <div class="container">
        <div class="box">

            <div class="field">
                <form class="ui form" action="/artworks/<%=artwork._id%>?_method=PUT" method="POST" enctype="multipart/form-data">
                    <div class="control" >
                        <input id="input" class="input one change" name="title" value="<%=artwork.title%>" placeholder="New post title here..." type="text">
                        <label class="file-label">
                            <input class="file-input" type="file" accept="image/*" name="image">
                            <span class="file-cta">
                                <span class="file-icon">
                                <i class="fas fa-upload"></i>
                                </span>
                                <span class="file-label">
                                Upload Image…
                                </span>
                            </span>
                            </label>
                        <textarea id="input" placeholder="Write your post content here..." class="textarea one chang" name="content" id="" cols="30" rows="10"><%=artwork.content%></textarea>
                        <button type="submit" class="button is-link">Publish</button>
                    </div>
                </form>
            </div>

        </div>
    </div>
</div>



<%-include("../partials/footer")%>
